{{!-- <form method="POST" action="/api/list/create?_method=PUT"> --}}
<div class="main-content">
  {{!-- <div class="profile-name"> --}}
    <div class="row mt-4 listForm">
    <h1>Welcome, {{name}}!</h1>
        <form class="form new-list-form">
          <div class="form-group">
      <h2>Create a New List:</h2>
            <label for="list-name">list name:</label>
            <input class="form-input" type="text" id="list-name" name="list-name" />
          </div>
        
          <div class="form-group">
            <button type="submit" href="views/create" class="btn btn-primary" id="save-Item">Create</button>
          </div>
        </form>
    </div>
  </div>
</div>
  {{!-- </form> --}}


  {{!-- {{#if lists.length}}
  <div class="col-md-6 list-list">
    <h3>Current Lists:</h3>

    {{#each lists as |list|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/list/{{list.id}}">{{list.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{list.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>
</div> --}}

<script src="./js/profile.js"></script>

